# AutoEditor

Guidance for working on the AutoEditor project.

- Read the relevant specification in `docs/` before changing behavior.
- Keep generated media and run artifacts under `workspace/`.
- Run `uv run ruff check .`, `uv run mypy src`, and `uv run pytest` before publishing changes.
